import 'package:flutter/material.dart';

class Infirmary extends StatelessWidget {
  const Infirmary({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
        'Infirmary'
    );
  }
}
