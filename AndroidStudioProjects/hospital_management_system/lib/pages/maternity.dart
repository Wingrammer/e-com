import 'package:flutter/material.dart';

class Maternity extends StatelessWidget {
  const Maternity({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
        'Maternity'
    );
  }
}
