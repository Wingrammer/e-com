import 'package:flutter/material.dart';

class InfirmaryRightArea extends StatelessWidget {
  const InfirmaryRightArea({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
        'InfirmaryRightArea'
    );
  }
}
