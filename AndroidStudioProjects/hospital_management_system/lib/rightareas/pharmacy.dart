import 'package:flutter/material.dart';

class PharmacyRightArea extends StatelessWidget {
  const PharmacyRightArea({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
        'PharmacyRightArea'
    );
  }
}
