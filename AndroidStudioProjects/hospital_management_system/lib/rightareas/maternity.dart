import 'package:flutter/material.dart';

class MaternityRightArea extends StatelessWidget {
  const MaternityRightArea({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
        'MaternityRightArea'
    );
  }
}
