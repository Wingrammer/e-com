import 'package:flutter/material.dart';

class StoreRightArea extends StatelessWidget {
  const StoreRightArea({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
        'StoreRightArea'
    );
  }
}
