import 'package:flutter/material.dart';

class LaboratoryRightArea extends StatelessWidget {
  const LaboratoryRightArea({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
        'LaboratoryRightArea'
    );
  }
}